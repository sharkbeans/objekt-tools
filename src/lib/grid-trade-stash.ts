// sessionStorage key used to hand off a poster draft from the grid board's
// "Trade" dialog (/collection/[nickname]/[member]) to the poster builder
// (/list?prefill=grid).
export const GRID_TRADE_STASH_KEY = "grid-trade-stash";
